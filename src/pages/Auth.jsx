import React from 'react'
import {Footer, HeaderAuth, MainAuth} from '../components'

const Auth = () => {
  return (
    <div>
        <HeaderAuth />
        <MainAuth />
        <Footer />
    </div>
  )
}

export default Auth